# === Core ===
flask>=3.0,<4.0
flask-sqlalchemy>=3.1,<4.0
flask-login>=0.6,<1.0
flask-wtf>=1.2,<2.0
wtforms>=3.1,<4.0
flask-compress>=1.17,<2.0
flask-limiter>=3.5,<4.0
apscheduler>=3.10,<4.0
alembic>=1.13,<2.0
flasgger>=0.9,<1.0

# === Data & Export ===
openpyxl>=3.1,<4.0
reportlab>=4.2,<5.0
arabic-reshaper>=3.0,<4.0
python-bidi>=0.6,<1.0
python-barcode>=0.15,<1.0

# === HTTP & Env ===
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0

# === Database backends ===
# Optional: PostgreSQL support (set DATABASE_URL env var). SQLite remains the default.
psycopg2-binary>=2.9,<3.0

# === Werkzeug (pinned for flask-login compat) ===
werkzeug>=3.0,<4.0

# === Production Server ===
waitress>=3.0,<4.0

# === Desktop Window ===
eel>=0.18,<1.0
pywin32>=306,<313
Pillow>=10.0,<11.0
psutil>=5.9,<8.0

# === Thermal Printing ===
python-escpos==3.1

# === Dev / Test ===
pytest>=9.0,<10.0
pytest-cov>=7.0,<8.0
pyinstaller>=6.0,<7.0
